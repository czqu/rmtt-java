<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>net.czqu.rmtt</groupId>
        <artifactId>rmtt-java-parent</artifactId>
        <version>1.0.1</version>
    </parent>
    <artifactId>rmtt-it</artifactId>
    <name>rmtt-it</name>
    <description>End-to-end integration tests: real client/server over TCP plus a
        cross-language interop entry point consumed by the rmtt-go client.</description>

    <dependencies>
        <dependency>
            <groupId>net.czqu.rmtt</groupId>
            <artifactId>rmtt-server-netty</artifactId>
        </dependency>
        <dependency>
            <groupId>net.czqu.rmtt</groupId>
            <artifactId>rmtt-client-netty</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <!-- self-signed certificates for the WSS/QUIC tests (netty's SelfSignedCertificate
                 relies on sun.misc internals unavailable on modern JDKs) -->
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>1.78.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
              End-to-end integration tests run in the verify phase via failsafe
              (*IT classes); surefire only picks up unit tests (*Test).
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.2.5</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
              Shade the server entry point into a self-contained runnable jar so the
              cross-language CI job can start it with `java -jar rmtt-e2e-server.jar`.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <finalName>rmtt-e2e-server</finalName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>net.czqu.rmtt.it.E2eServerMain</mainClass>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
